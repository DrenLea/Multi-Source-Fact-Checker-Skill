# Multi-Source Fact Checker Skill
# Dependencies for the Pharos + Anvita Flow Hackathon project

# Async & HTTP
aiohttp>=3.9.0
requests>=2.31.0
httpx>=0.25.0

# Web3 / Social Graph
lens-py-sdk>=0.1.0
farcaster-py>=0.2.0

# Fact-checking
# loki  # Uncomment when loki is available on PyPI; for now, use mock

# ML / Inference
torch>=2.1.0
transformers>=4.36.0
# paladin-mini model is downloaded on first use from HuggingFace

# Utilities
loguru>=0.7.0
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.5.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
